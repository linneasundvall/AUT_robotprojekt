.PROGRAM shift(off_x, off_y)

        summa_y = summa_y+(off_y)
        summa_x = summa_x+(off_x)

        SET flytta = SHIFT(labyrint_start BY summa_x,summa_y)
        MOVES flytta
        BREAK
        WAIT.EVENT 0.5



.END

